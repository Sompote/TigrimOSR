# Tigrimos subapp (separate project)
Tigrimos/

# Tiger cowork (TypeScript original)
tiger_cowork/

# Rust build artifacts
/target/
Cargo.lock

# Data directories (user-specific, not part of source)
data/checkpoints/
data/plugins.json
data/plugins/
data/transcripts/
data/activity_logs/
data/sandbox/
data/skills/
data/agents/
data/file_tokens.json
data/remote_cache/
data/chat_logs/
data/chat_history.json
data/agent_history/
data/sessions/
data/settings.json
data/projects.json
data/projects/
data/tasks.json
data/skills.json
data/skills/
data/file_tokens.json
data/transcripts/
data/debug_*.txt
output_file/
sandbox/

# macOS
.DS_Store
*.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
nohup.out

# Environment / secrets
.env
*.key
ecosystem.config.js

# Build / install artifacts
dist/
*.deb

# Local orchestrator persona files
data/IDENTITY.md
data/SOUL.md

# Upstream checkouts kept locally as integration reference material. What was
# worth taking from them lives in src/ and assets/ as first-class code; the
# checkouts themselves are not part of this product and are not vendored.
Repos/
agent-graph/

# Runtime state written by the agent loop and tool editor.
# NOTE: assets/agents/ is deliberately NOT ignored — those YAML team files are
# include_str!'d into the binary by src/server/services/agent_library.rs, so the
# build fails without them.
data/agent_loops/
data/tools/
